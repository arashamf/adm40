#include "stm32f4xx_hal.h"
#include "gpio.h"
#include "pins.h"
#include "timer.h"

/*
void indic(int n)
{

 int i;                 
 for(i=0; i<n; i++)
  {
	VLED_EN(0);
 	delay_ms(200);
	VLED_EN(1);
	delay_ms(200);
  }

}
*/
